# Copy to .env — API URL for the Laravel backend
# Optional. In local dev, leave unset — Vite proxies /api to http://127.0.0.1:8000.
# VITE_API_BASE_URL=http://127.0.0.1:8000

# AI runs on the server only. Add GOOGLE_API_KEY to backend/.env (never here).
